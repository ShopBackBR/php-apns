{
    "websiteName": "{{ websiteName }}",
    "websitePushID": "{{ websitePushId }}",
    "allowedDomains": ["http://{{ host }}", "https://{{ host }}", "https://{{ pushSubDomain }}"],
    "urlFormatString": "http://{{ host }}/%@",
    "authenticationToken": "{{ userId }}",
    "webServiceURL": "https://{{ webServiceHost }}/safari_push_notifications/{{ clientId }}/{{ userId }}"
}
